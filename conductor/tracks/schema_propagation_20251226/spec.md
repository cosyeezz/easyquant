# Spec: 元数据传播引擎实现 (Schema Propagation)

## 目标
核心功能是实现“列级感知”。当用户连接两个节点时，下游节点应自动获得上游节点输出的字段列表（Schema），并能基于这些字段进行后续配置。

## 核心需求
1. **后端推导引擎:** 在 `runner.py` 或独立服务中实现根据节点配置推导输出 Schema 的逻辑。
2. **前端数据流同步:** 当连线发生变化时，触发前端的状态更新，将上游的 `outputs_schema` 传递给下游的 `inputs_schema`。
3. **类型安全检查:** 自动验证上下游连接的兼容性。
